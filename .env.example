APP_SECRET=replace-this-for-hosted-demo
APP_DEBUG=0
# Comma-separated list of origins allowed for state-changing API calls (CSRF guard).
# After deploying, set this to your hosted URL (e.g. https://atomquest.onrender.com).
APP_ORIGIN=http://127.0.0.1:8000,http://localhost:8000

